#include "main.h"

/**
 * _isupper - check for uppercase letter.
 * @c : input data
 * Return: 0 if lower case, 1 uppercase.
 */

int _isupper(int c)
{
	if (c < 'A' || c > 'Z')
	{
	return (0);
	}
	else
	return (1);

}
