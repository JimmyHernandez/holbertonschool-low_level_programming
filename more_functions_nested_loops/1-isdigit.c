#include "main.h"

/**
 * _isdigit - check for number 0-9.
 * @c : input data
 * Return: 0 if a letter, 1 is digit.
 */

int _isdigit(int c)
{
	if (c < '0' || c > '9')
	{
	return (0);
	}
	else
	return (1);

}

